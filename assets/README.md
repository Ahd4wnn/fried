# Brand assets

Drop the Hovio logo files here:

| File             | Use                                                  |
| ---------------- | ---------------------------------------------------- |
| `logo.png`       | **Dark** logo — for light backgrounds (cream/paper)  |
| `logo-white.png` | **White** logo — for dark backgrounds (forest-deep)  |

All logo references across the app point to this folder. Keep the filenames
exactly as above so nothing breaks. PNG with transparent background, please.

> These two files are intentionally not committed yet — add them here. The
> `.gitkeep` keeps this folder tracked until they arrive.
